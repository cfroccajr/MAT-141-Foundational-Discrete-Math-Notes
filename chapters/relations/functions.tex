\subsection*{Functions as Relations}
% Define functions as associations between sets and as relations.
\begin{defn}[Function]\index{function}\index{domain}\index{codomain}\index{range}
\index{function!domain}\index{function!codomain}\index{function!range}
    A \emph{function} is a relation between two sets, the first called the \emph{domain} and the second the \emph{codomain}, such that for each \(x\) in the domain there is {\em exactly} one \(y\) in the codomain associated with \(x\).  Further, the set of all \(y\) from the codomain which are associated with at least one \(x\) in the domain is called the \emph{range}.
\end{defn}


% Domain, Co-Domain, Range/Image
\begin{exposition}\phantom{stuff}
\begin{sides}
    Define a function \(f\) from the Domain \[D=\{a,b,c,d\}\] to the Codomain \[C=\{1,2,3,4,5\}\] with the following set of pairs:
    \[f=\left\{(a,3),(b,4),(c,2),(d,5)\right\}.\]
    Since there are no letters associated with the \(1\in C\), the Range is
    \begin{align*}
        R&=\{y\in C|\exists x\in D: f(x)=y\}\\
            &=\{2,3,4,5\}.
    \end{align*}
\tcblower
    \centering
    % \inputtikz{function_image_example}
    \includegraphics[scale=0.24,alt={}]{images/created/function_image_example.png}
\end{sides}
\end{exposition}

% Exmaple given set draw diagram
\begin{practiceprob}\phantom{stuff}
\begin{sides}
    Define a function \(S\) from the Primes \[P=\{2,3,5,7,11,\ldots\}\] to the Alphabet \[A=\{a,b,c,d,e,\ldots,w,x,y,z\}\] with the following set of pairs:
    \[S=\left\{(2,w),(3,h),(5,i),(7,e),(11,l),\ldots\right\}.\]
\tcblower
    \centering
    % \inputtikz{blank_function_diagram}
    \includegraphics[scale=0.24,alt={}]{images/created/blank_function_diagram.png}
\end{sides}
\end{practiceprob}

\clearpage

% Example given diagram give set of pairs

\begin{practiceprob}\phantom{stuff}
\begin{sides}
    What ordered pairs are in the function illustrated on the right?\vspace{5\baselineskip}

    What is the range of the illustrated function?
\tcblower
    \centering
    % \inputtikz{function_prob}
    \includegraphics[scale=0.24,alt={}]{images/created/function_prob.png}    
\end{sides}
\end{practiceprob}


% Example to discuss function Notation

\begin{practiceprob} Recall that \emph{function notation} like \(S(x)\) is read ``\(S\) of \(x\)'' and whatever we replace \(x\) with on the left, we do the same on the right.\index{function notation}
\begin{sides}
    Define
    \begin{equation*}
        S(x) = 1/\sqrt{x^2+1}
    \end{equation*}
    Evaluate the function at selection of points in the domain \[D=(-4,4)\subset\bbR.\]
\tcblower
    \centering
    % \inputtikz{function_algebraic}
    \includegraphics[scale=0.24,alt={}]{images/created/function_algebraic.png}    
\end{sides}
\end{practiceprob}

% Example to discuss function Notation

\begin{practiceprob} This is an example of a \emph{piecewise defined} function.
\index{function!piecewise defined}\index{piecewise defined}
\begin{sides}
    Define
    \begin{equation*}
        P(x)=
        \left\{
            \begin{array}{ll}
                0 & if\ x\ is\ not\ prime \\
                1 &  if\ x\ is\ prime
            \end{array}
        \right.
    \end{equation*}
    Evaluate the function at each point in the domain \[D=\{1,2,3,5,6,7,8,9,10\}.\]
\tcblower
    \centering
    % \inputtikz{function_piecewise}
    \includegraphics[scale=0.24,alt={}]{images/created/function_piecewise.png}
\end{sides}
\end{practiceprob}


\clearpage

\subsection*{Properties of Functions}

\begin{defn}[Properties of Functions]\index{one-to-one}\index{onto}\index{bijection}
\index{function!one-to-one}\index{function!onto}\index{function!bijection}
    A function \(f:A\longrightarrow B\) is said to be \emph{one-to-one} (\(1-1\)) if for all \(y\) in the range there is exactly one \(x\) in the domain such that \(f(x)=y\).  The function is \emph{onto} if for all \(y\) in the codomain there is at least one \(x\) in the domain such that \(f(x)=y\).  If a function is both one-to-one and onto we say that it is a \emph{bijection}.
    \begin{itemize}
        \item One-to-One: \(\forall x_0,x_1\in A: f(x_0)=f(x_1) \Longrightarrow x_0=x_1\)
        \item Onto: \(\forall y\in B\, \exists x\in A: f(x)=y\)
    \end{itemize}
\end{defn}

% One-to-One, Onto, Bijections

\begin{practiceprob}\label{prob:functionprop}
    For each function circle the correct description: 1-1, onto, both, or neither.
\begin{figure}[H]
    \centering
% just onto
    \begin{subfigure}{0.45\textwidth}
    \centering
    % \inputtikz{function_ontoonly}
    \includegraphics[scale=0.24,alt={}]{images/created/function_ontoonly.png}
    \caption{1-1, onto, both, or neither}
    \label{fig:ontoonly}
    \end{subfigure}
% neither
    \begin{subfigure}{0.45\textwidth}
    \centering
    % \inputtikz{function_neither}
    \includegraphics[scale=0.24,alt={}]{images/created/function_neither.png}
    \caption{1-1, onto, both, or neither}
    \label{fig:neither}
    \end{subfigure}

\vspace{\baselineskip}

% just both
    \begin{subfigure}{0.45\textwidth}
    \centering
    % \inputtikz{function_both}
    \includegraphics[scale=0.24,alt={}]{images/created/function_both.png}
    \caption{1-1, onto, both, or neither}
    \label{fig:both}
    \end{subfigure}
% just one-to-one
    \begin{subfigure}{0.45\textwidth}
    \centering
    % \inputtikz{function_onetoone}
    \includegraphics[scale=0.24,alt={}]{images/created/function_onetoone.png}
    \caption{1-1, onto, both, or neither}
    \label{fig:onetooneonly}
    \end{subfigure}    
    \caption{Function Properties Practice}
    \label{fig:function_prob_practice}
\end{figure}
        
\end{practiceprob}

\begin{thm}\index{cardinality}
    A function between two finite sets of the same size, cardinality, is either both one-to-one and onto, or neither one-to-one nor onto.
\end{thm}

\clearpage

\begin{practiceprob}
Is the below function 1-1, onto, both, or neither?  The answer depends on the {\em domain} and {\em codomain}.  For each description, let \(S\) be the set of points on the spiral in figure \ref{fig:spiral}.
    \begin{enumerate}
        \item {\em Domain:} \(\bbR\), {\em Codomain} \(\bbR\): \(f(x)=y,\ \text{if \((x,y)\in S\)}\)\vspace{3.6\baselineskip}
        \item {\em Domain:} \(\bbR\), {\em Codomain} \(\mathscr{P}(\bbR)\): \(f(x)=\left\{y\middle|(x,y)\in S\right\}\)\vspace{3.6\baselineskip}
        \item {\em Domain:} \(\bbR^+\), {\em Codomain} \(\bbR\times \bbR\):
            \(f(t)=\left<\frac{t}{2\pi}\cdot\cos(t),\frac{t}{2\pi}\cdot\sin(t)\right>\)\vspace{3.6\baselineskip}
        \item {\em Domain:} \(S\), {\em Codomain} \(\bbR^+\): for \(s\in S\),
            \(f(s)=\text{length of the spiral up to \(s\)}\)\vspace{3.6\baselineskip}
    \end{enumerate}
    \begin{figure}[H]
        \centering
        % \inputtikz{function_spiral}
	\includegraphics[scale=0.24,alt={}]{images/created/function_spiral.png}
        \caption{Points on a Spiral}
        \label{fig:spiral}
    \end{figure}
\end{practiceprob}

\clearpage

\begin{defn}[Inverses]\index{function!inverse}\index{relation!inverse}
    Given a relation \(R\) define the \emph{inverse} of \(R\) to be the set \[R^{-1}=\left\{(y,x)\middle|(x,y)\in R\right\}.\]
    If \(R\) and \(R^{-1}\) are both functions, then we say \(R\) is \emph{invertible}.
\end{defn}

\begin{practiceprob}
    For each function in practice block \ref{prob:functionprop} on \pageref{prob:functionprop}, write the function and its ``inverse'' as sets of ordered pairs \(R\) and \(R^{-1}\).  Which functions are invertible?  What other property do all the invewrtible functions have?
    \begin{itemize}
    \foreach \exmp in {1,...,4}{
        \item[]Example \exmp:\vspace{2.3\baselineskip}
    }
    \end{itemize}
\end{practiceprob}

\begin{thm}[Invertible Functions]
    A function will be invertible if and only if it is \hrulefill\phantom{T} 
\end{thm}

% \clearpage

% Composing Functions

\begin{defn}[Function Composition]\index{function!composition}
    Given a function \(f:A\rightarrow B\) and \(g:B\rightarrow C\), the \emph{composition} of \(f\) and \(g\) is a function from the domain of \(f\) to the codomain of \(g\) defined by \(g\circ f(x)=g(f(x))\) and we read \(g\circ f\) as ``\(g\) compose \(f\).''
    \begin{figure}[H]
        \centering
        % \inputtikz{function_composition0}
	\includegraphics[scale=0.24,alt={}]{images/created/function_composition0.png}
        \caption{Schematic of Function Composition}
        \label{fig:function_comp_def}
    \end{figure}
    Note, this is well defined as long as the range of \(f\) is a subset of the domain of \(g\).
\end{defn}

\begin{practiceprob}
    For each image decide if \(g\circ f(x)\)\  1-1, onto, both, or neither?  How does this compare to \(f\) and \(g\) individually?
    \begin{figure}[H]
        \centering
        \begin{subfigure}{0.45\textwidth}
            \inputtikz{function_composition2}
        	% \includegraphics[scale=0.24,alt={}]{images/created/function_composition2.png}
            \caption{1-1, onto, both, or neither}
            \label{fig:composition2}
        \end{subfigure}
        \begin{subfigure}{0.45\textwidth}
            \inputtikz{function_composition1}
        	% \includegraphics[scale=0.24,alt={}]{images/created/function_composition1.png}
            \caption{1-1, onto, both, or neither}
            \label{fig:composition1}
        \end{subfigure}

\vspace{6\baselineskip}

        \begin{subfigure}{0.45\textwidth}
            \inputtikz{function_composition3}
        	% \includegraphics[scale=0.24,alt={}]{images/created/function_composition3.png}
            \caption{1-1, onto, both, or neither}
            \label{fig:composition3}
        \end{subfigure}
        \begin{subfigure}{0.45\textwidth}
            \inputtikz{function_composition4}
        	% \includegraphics[scale=0.24,alt={}]{images/created/function_composition4.png}
            \caption{1-1, onto, both, or neither}
            \label{fig:composition4}
        \end{subfigure}
        \caption{Composed Functions}
        \label{fig:function_comp}
    \end{figure}
\vspace{6\baselineskip}
\end{practiceprob}

\begin{thm}
    The composition of functions \(g\circ f\) is one-to-one only if \(f\) is one-to-one and it is onto only if \(g\) is onto.
\end{thm}

\clearpage

\begin{practiceprob}
    Using \(f(x)=x^2\), \(g(x)=\sqrt{x}\), and \(h(x)=x+1\), find each of the following:
    \begin{enumerate}
        \item \(f\circ h(x)=\)\vspace{4\baselineskip}
        \item \(h\circ f(x)=\)\vspace{4\baselineskip}
        \item \(f\circ g(x)=\)\vspace{4\baselineskip}
        \item \(g\circ f(x)=\)\vspace{4\baselineskip}
    \end{enumerate}
\end{practiceprob}