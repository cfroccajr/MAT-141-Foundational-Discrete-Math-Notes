% \(A=\left\{1,3,5,7,9\right\}\) and \(B=\left\{2,3,5,7\right\}\)

\begin{tikzpicture}[scale=0.5]
% Axes
    \draw[dashed,ultra thin,gray] (0,0) grid (9.5,7.5);
    \draw[->] (0,0) -- (10,0);
    \draw[->] (0,0) -- (0,8);
% Ordered Pairs
    \foreach \a in {1,3,5,7,9}{
        \foreach \b in {2,3,5,7}{
            \shade[ball color=accent] (\a,\b) circle (7pt);
            \node[anchor=north] at (\a,0) {\a};
            \node[anchor=east] at (0,\b) {\b};
        };
    };
\end{tikzpicture}