\begin{tikzpicture}[scale=0.85, transform shape]
% Boxes
    \def\n{5};
    \foreach \r in {1,...,\n}{
    % First Sum
        \fill[color=accent,opacity=0.25] (0,\r) rectangle (\r,\r+1);
        \draw[color=accent] (0,\r) grid (\r,\r+1);
    % Second Sum
        \fill[color=secondaryaccent,opacity=0.25] (\r+2,\r) rectangle (\n+3,\r+1);
        \draw[color=secondaryaccent] (\r+2,\r) grid (\n+3,\r+1);
    % Total
        \fill[color=accent,opacity=0.25] (\n+5,\r) rectangle (\n+5+\r,\r+1);
        \fill[color=secondaryaccent,opacity=0.25] (\n+\r+5,\r) rectangle (2*\n+6,\r+1);
        \draw[color=accent] (\n+5,\r) grid (\n+5+\r,\r+1);
        \draw[color=secondaryaccent] (\n+\r+5,\r) grid (2*\n+6,\r+1);
    };
% Labels
    \draw[decorate,decoration={brace,amplitude=10pt}] (2*\n+6,1+\n) -- (2*\n+6,1)
        node[pos=0.5,right,xshift=10pt]{\large \(n\)};
    \draw[decorate,decoration={brace,amplitude=10pt}] (\n+5,1+\n) -- (2*\n+6,1+\n)
        node[pos=0.5,above,yshift=10pt]{\large \(n+1\)};
    \node at (\n+4,0.5*\n+1) {\large \(=\)};
    \node at (0.5*\n+1.5,0.5*\n+1) {\large \(+\)};
\end{tikzpicture}