\usetikzlibrary{graphs.standard,graphs,graphdrawing,trees,quotes}
\usegdlibrary{trees,layered,force,circular}
% \usetikzlibrary{decorations.pathmorphing}
\usepackage{blkarray}
\usepackage{mathtools}

\usetikzlibrary{decorations.pathmorphing}

\tcbset{
    probstyle/.style={
       colframe=secondaryaccent,
       colback=white,
       opacityback=1,
       before skip=10pt,
       after skip=10pt,
    },
}

\tcbset{
    exposstyle/.style={
       colframe=accent,
       colback=white,
       opacityback=1,
       before skip=10pt,
       after skip=10pt,
    },
}

% See:
% https://mirrors.ibiblio.org/pub/mirrors/CTAN/macros/latex/required/amscls/doc/amsthdoc.pdf
% \newtheoremstyle{note}% hnamei
%     {3pt}% Space above
%     {3pt}% Space below
%     {}% Body font
%     {}% Indent amount
%     {\itshape}% Theorem head font
%     {:}% Punctuation after theorem head
%     {.5em}% Space after theorem head
%     {}% Theorem head spec (can be left empty, meaning ‘normal’)
\newtheoremstyle{break}%
    {}{}%
    {\rmfamily}{}%
    {\bfseries}{}% % Note that final punctuation is omitted.
    {\newline}
    {\thmname{#1}\thmnumber{ #2}: \thmnote{#3}}

% \newtheoremstyle{definition}%
%     {}{}%
%     {\rmfamily}{}%
%     {\bfseries}{}% % Note that final punctuation is omitted.
%     {\newline}
%     {\thmname{#1}\thmnumber{ #2}: \thmnote{#3}}

% \newtheoremstyle{theorem}%
%     {}{}%
%     {\itshape}{}%
%     {\bfseries}{}% % Note that final punctuation is omitted.
%     {\newline}
%     {\thmname{#1}\thmnumber{ #2}: \thmnote{#3}}

\theoremstyle{definition}
\newtheorem{practiceprob}{Practice Problem}
\newtheorem{exposition}{Exposition}

\tcolorboxenvironment{practiceprob}{probstyle}
\tcolorboxenvironment{exposition}{exposstyle}

\newtcolorbox{sides}[1][]{
    sidebyside,
    sidebyside align=top,
    colback=white,
    frame empty,
    #1,
}

\numberwithin{thm}{chapter}

\numberwithin{note}{chapter}

\numberwithin{defn}{chapter}
\numberwithin{axiom}{chapter}
\numberwithin{exmp}{chapter}
\numberwithin{exer}{chapter}


\numberwithin{practiceprob}{chapter}
\numberwithin{exposition}{chapter}


% New Styling
\tikzset{
    backline/.style={
        line width=3pt,color=white,shorten <=3pt,shorten >=6pt,line cap=round
    }
}
\tikzset{
    frontline/.style={
        color=black,shorten <=0pt,shorten >=0pt,thick,line cap=rect
    }
}
\tikzset{
    doublestruck/.style = {
        postaction={draw,frontline},backline
    }
}


\tikzset{
    vertex/.style={
        shade,
        ball color=white,
        circle,
        line width=1
    }
}
\tikzset{
    SolidVertex/.style={
        shade,shading=ball,
        circle,
        ball color=black!80!white,
        minimum size=3pt
    }
}
\tikzset{
    edges/.style={
        line width=1.5,
        every loop/.style={-},
        shorten <= 1.5pt,
        shorten >= 1.5pt,
    }
}


% postaction={draw,line width=0.35mm,red,dash pattern=on 3pt off 5pt,dash phase=4pt,thick},
%         blue,dash pattern= on 3pt off 5pt,thick,rounded corners}}


% Misc Note on converting images
% for file in *.pdf; do magick -density 300 "$file" "${file%.pdf}.png"; done
% Then scale the image to 24% when imported